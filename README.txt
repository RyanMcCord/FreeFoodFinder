SETUP:
  - download Node.js (https://nodejs.org/en/download/)
  - in command line /FreeFoodFinder:
      npm install
      npm install firebase
      npm install google-maps-react

RUN:
  - in command line /FreeFoodFinder:
      npm start
